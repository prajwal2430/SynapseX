export declare const servicesConfig: (() => {
    aiService: {
        url: string;
        apiKey: string;
        timeoutMs: number;
    };
}) & import("@nestjs/config").ConfigFactoryKeyHost<{
    aiService: {
        url: string;
        apiKey: string;
        timeoutMs: number;
    };
}>;
